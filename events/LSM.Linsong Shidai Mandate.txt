add_namespace = LSM
	country_event = { # Context of the Past / Introduction
		id = LSM.0
		title = LSM.0.t
		desc = LSM.0.d
		picture = GFX_report_event_usa_election_generic
		is_triggered_only = yes

		immediate = { log = "[GetDateText]: [Root.GetName]: Event LSM.0" }

		option = {
			name = LSM.0.OptA
		}
	}

	country_event = { # First Event
		id = LSM.1
		title = LSM.1.t
		desc = LSM.1.d
		picture = GFX_report_event_usa_election_generic
		is_triggered_only = yes

		immediate = { log = "[GetDateText]: [Root.GetName]: Event LSM.1" }

		option = {
			name = LSM.1.OptA
		}

		option = {
			name = LSM.1.OptB
		}
	}

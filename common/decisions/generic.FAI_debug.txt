FAI_decision_debug = { # Fantasy AI Debug
	FIA_debug_electrical_grid_queuing = {
		available = { is_debug = yes }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FIA_debug_electrical_grid_queuing"
			FAI_electrical_grid_queuing = yes
		}

		ai_will_do = { factor = 0 }
	}

	FIA_debug_commercial_nuclear_reactor_queuing = {
		available = { is_debug = yes }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FIA_debug_commercial_nuclear_reactor_queuing"
			FAI_commercial_nuclear_reactor_queuing = yes
		}

		ai_will_do = { factor = 0 }
	}

	FIA_debug_air_base_queuing = {
		available = { is_debug = yes }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FIA_debug_air_base_queuing"
			FAI_air_base_queuing = yes
		}

		ai_will_do = { factor = 0 }
	}

	FIA_debug_calculate_resource_status = {
		available = { is_debug = yes }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FAI_calculate_resource_status"
			FAI_calculate_resource_status = yes
			log = "[THIS.GetName]: HOW the VAR's Work and gather numbers:"
			log = "    resource@oil = [?resource@oil]"
			log = "    resource_consumed@oil = [?resource_consumed@oil]"
			log = "    resource_exported@oil = [?resource_exported@oil]"
			log = "    resource_imported@oil = [?resource_imported@oil]"
			log = "    resource_produced@oil = [?resource_produced@oil]"
			log = "    resource@aluminium = [?resource@aluminium]"
			log = "    resource_consumed@aluminium = [?resource_consumed@aluminium]"
			log = "    resource_exported@aluminium = [?resource_exported@aluminium]"
			log = "    resource_imported@aluminium = [?resource_imported@aluminium]"
			log = "    resource_produced@aluminium = [?resource_produced@aluminium]"
			log = "    resource@rubber = [?resource@rubber]"
			log = "    resource_consumed@rubber = [?resource_consumed@rubber]"
			log = "    resource_exported@rubber = [?resource_exported@rubber]"
			log = "    resource_imported@rubber = [?resource_imported@rubber]"
			log = "    resource_produced@rubber = [?resource_produced@rubber]"
			log = "    resource@tungsten = [?resource@tungsten]"
			log = "    resource_consumed@tungsten = [?resource_consumed@tungsten]"
			log = "    resource_exported@tungsten = [?resource_exported@tungsten]"
			log = "    resource_imported@tungsten = [?resource_imported@tungsten]"
			log = "    resource_produced@tungsten = [?resource_produced@tungsten]"
			log = "    resource@steel = [?resource@steel]"
			log = "    resource_consumed@steel = [?resource_consumed@steel]"
			log = "    resource_exported@steel = [?resource_exported@steel]"
			log = "    resource_imported@steel = [?resource_imported@steel]"
			log = "    resource_produced@steel = [?resource_produced@steel]"
			log = "    resource@chromium [?resource@chromium]"
			log = "    resource_consumed@chromium [?resource_consumed@chromium]"
			log = "    resource_exported@chromium [?resource_exported@chromium]"
			log = "    resource_imported@chromium [?resource_imported@chromium]"
			log = "    resource_produced@chromium [?resource_produced@chromium]"
			log = "    resource@coal = [?resource@coal]"
			log = "    resource_consumed@coal = [?resource_consumed@coal]"
			log = "    resource_exported@coal = [?resource_exported@coal]"
			log = "    resource_imported@coal = [?resource_imported@coal]"
			log = "    resource_produced@coal = [?resource_produced@coal]"
		}

		ai_will_do = { factor = 0 }
	}
}

HHE_front_management_rush_HER = {
    allowed = { TAG = HHE }
    enable = { has_war_with = HER }

    abort_when_not_enabled = yes
    
    ai_strategy = {
        type = front_control

        country_trigger = { TAG = HER }

        priority = 135
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
}

HHE_front_management_rush_HFS = {
    allowed = { TAG = HHE }
    enable = {
        has_war_with = HFS
        NOT = {
            # Minor Major
            country_exists = HER
            country_exists = HSP
            # Released
            country_exists = HFF
            country_exists = HFR
            country_exists = HFS
            country_exists = HPG
        }
    }

    abort_when_not_enabled = yes
    
    ai_strategy = {
        type = front_control

        country_trigger = {
            enemies_strength_ratio < 2.0
            TAG = HFS
        }

        priority = 135
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
}

HHE_front_management_rush_HFS_double = {
    allowed = { TAG = HHE }
    enable = {
        date > 1728.1.1
    }

    abort_when_not_enabled = yes
    
    ai_strategy = {
        type = front_control

        country_trigger = {
            TAG = HFS
        }

        priority = 65
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
}

HHE_front_management_rush_weak_minor = {
    allowed = {
        OR = {
            TAG = HHE
            TAG = HLF
            TAG = HNO
            TAG = HST
        }
    }
    enable = {
        OR = {
            has_global_flag = HHE_warmonger_civil_war_final
            AND = {
                TAG = HHE
                check_variable = { 183 > days_decision_timeout@mission_HHE_collapse_of_the_nation_civil_war_full  }
            }
            TAG = HLF
            TAG = HNO
            TAG = HST
        }
        has_war = yes
    }

    abort_when_not_enabled = yes
    
    ai_strategy = {
        type = front_control

        country_trigger = {
            enemies_strength_ratio < 1.6
            OR = {
                # Minor Major
                TAG = HER
                TAG = HSP
                # Released
                TAG = HFF
                TAG = HFR
                TAG = HFS
                TAG = HPG
            }
        }

        priority = 135
        ordertype = front
        execution_type = rush_weak
        execute_order = yes
        manual_attack = yes
    }
}

HHE_front_management_rush_weak_minor_double = {
    allowed = {
        OR = {
            TAG = HHE
            TAG = HLF
            TAG = HNO
            TAG = HST
        }
    }
    enable = {
        date > 1728.1.1
    }

    abort_when_not_enabled = yes
    
    ai_strategy = {
        type = front_control

        country_trigger = {
            OR = {
                # Minor Major
                TAG = HER
                TAG = HSP
                # Released
                TAG = HFF
                TAG = HFR
                TAG = HFS
                TAG = HPG
            }
        }

        priority = 65
        ordertype = front
        execution_type = rush_weak
        execute_order = yes
        manual_attack = yes
    }
}

HHE_front_management_rush_weak_major = {
    allowed = {
        OR = {
            TAG = HHE
            TAG = HLF
            TAG = HNO
            TAG = HST
        }
    }
    enable = {
        OR = {
            has_global_flag = HHE_warmonger_civil_war_final
            OR = {
                AND = {
                    TAG = HHE
                    HFF = {
                        OR = {
                            exists = no
                            has_capitulated = yes
                        }
                    }
                    HFR = {
                        OR = {
                            exists = no
                            has_capitulated = yes
                        }
                    }
                    HPG = {
                        OR = {
                            exists = no
                            has_capitulated = yes
                        }
                    }
                }
                TAG = HLF
                TAG = HNO
                TAG = HST
            }
        }
        has_war = yes
    }

    abort_when_not_enabled = yes
    
    ai_strategy = {
        type = front_control

        country_trigger = {
            NOT = { enemies_strength_ratio > 1.7 }
            OR = {
                TAG = HHE
                TAG = HLF
                TAG = HNO
                TAG = HST
            }
        }

        priority = 165
        ordertype = front
        execution_type = rush_weak
        execute_order = yes
        manual_attack = yes
    }
}

HHE_front_management_rush_weak_major_double = {
    allowed = {
        OR = {
            TAG = HHE
            TAG = HLF
            TAG = HNO
            TAG = HST
        }
    }
    enable = {
        date > 1728.6.1
    }

    abort_when_not_enabled = yes
    
    ai_strategy = {
        type = front_control

        country_trigger = {
            OR = {
                TAG = HHE
                TAG = HLF
                TAG = HNO
                TAG = HST
            }
        }

        priority = 35
        ordertype = front
        execution_type = rush_weak
        execute_order = yes
        manual_attack = yes
    }
}

HHE_front_management_rush_central = {
    allowed = {
        OR = {
            TAG = HHE
            TAG = HST
        }
    }
    enable = {
        OR = {
            AND = {
                TAG = HHE
                controls_state = 2
                controls_state = 79
                controls_state = 81
                NOT = {
                    # Minor Major
                    country_exists = HER
                    country_exists = HSP
                    # Released
                    country_exists = HFF
                    country_exists = HFR
                    country_exists = HFS
                    country_exists = HPG
                }
            }
            TAG = HST
        }
        OR = {
            has_war_with = HLF
            has_war_with = HNO
        }
    }

    abort_when_not_enabled = yes
    
    ai_strategy = {
        type = front_control

        country_trigger = {
            NOT = { enemies_strength_ratio > 1.7 }
            OR = {
                TAG = HLF
                TAG = HNO
            }
        }

        priority = 165
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
}

HHE_front_management_rush_south_west = {
    allowed = {
        OR = {
            TAG = HHE
            TAG = HST
        }
    }
    enable = {
        OR = {
            AND = {
                TAG = HHE
                NOT = {
                    country_exists = HLF
                    country_exists = HNO
                    country_exists = HST
                    # Minor Major
                    country_exists = HER
                    country_exists = HSP
                    # Released
                    country_exists = HFF
                    country_exists = HFR
                    country_exists = HFS
                    country_exists = HPG
                }
            }
            TAG = HST
        }
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_control

        country_trigger = {
            NOT = { enemies_strength_ratio > 1.7 }
            OR = {
                TAG = HAW
                TAG = HCR
                TAG = HPP
                TAG = HWA
            }
        }

        priority = 165
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
}

HST_front_management_rush_weak_HAP = {
    allowed = { TAG = HST }
    enable = { has_war_with = HAP }

    abort_when_not_enabled = yes
    
    ai_strategy = {
        type = front_control

        country_trigger = { TAG = HAP }

        priority = 200
        ordertype = front
        execution_type = rush_weak
        execute_order = yes
        manual_attack = yes
    }
}

HPP_front_management_rush_HAW = {
    allowed = { TAG = HPP }
    enable = { has_war_with = HAW }

    abort_when_not_enabled = yes
    
    ai_strategy = {
        type = front_control

        country_trigger = { TAG = HAW }

        priority = 150
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
}

HPP_front_management_rush_weak_HCR = {
    allowed = { TAG = HPP }
    enable = {
        has_war_with = HCR
        NOT = { has_war_with = HAW }
    }

    abort_when_not_enabled = yes
    
    ai_strategy = {
        type = front_control

        country_trigger = { TAG = HCR }

        priority = 135
        ordertype = front
        execution_type = rush_weak
        execute_order = yes
        manual_attack = yes
    }
}

HHE_front_management_ignore_foreign = {
    allowed = {
        OR = {
            TAG = HHE
            TAG = HLF
            TAG = HNO
            TAG = HST
        }
    }
    enable = {
        has_global_flag = HHE_warmonger_civil_war_final
    }

    abort_when_not_enabled = yes
    
    ai_strategy = {
        type = front_control

        country_trigger = {
            OR = {
                TAG = REP
                TAG = DOC
                TAG = IHS
                TAG = WIE
                TAG = LRO
                TAG = LEE
                TAG = NMR
                TAG = PCS
                TAG = SMK
                TAG = KGK
                TAG = MRC
                TAG = CYR
            }
        }

        ratio = -10.0
        priority = -1000
        ordertype = front
    }
}
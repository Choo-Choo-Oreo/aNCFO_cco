MJU_front_management_rush_SPC = {
    allowed = { TAG = MJU }
    enable = { has_war_with = SPC }

    abort_when_not_enabled = yes
    
    ai_strategy = {
        type = front_control

        country_trigger = { TAG = SPC }

        priority = 135
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
}

MJU_front_management_rush_UPF = {
    allowed = { TAG = MJU }
    enable = { has_war_with = UPF }

    abort_when_not_enabled = yes
    
    ai_strategy = {
        type = front_control

        country_trigger = { TAG = UPF }

        priority = 200
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
}

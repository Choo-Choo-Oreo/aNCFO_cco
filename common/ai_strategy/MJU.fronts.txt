MJU_front_management_rush_SPC = {
    allowed = { TAG = MJU }
    enable = { has_war_with = SPC }

    abort_when_not_enabled = yes
    
    ai_strategy = {
        type = front_control

        country_trigger = { TAG = SPC }

        priority = 135
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
}

MJU_front_management_rush_CPF = {
    allowed = { TAG = MJU }
    enable = { has_war_with = CPF }

    abort_when_not_enabled = yes
    
    ai_strategy = {
        type = front_control

        country_trigger = { TAG = CPF }

        priority = 200
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
}

CPF_front_management_rush_MJU = {
    allowed = { TAG = CPF }
    enable = { has_war_with = MJU }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = garrison
        value = 20
    }

    ai_strategy = {
        type = front_control

        country_trigger = { TAG = MJU }

        ordertype = front
        execution_type = careful
        execute_order = no
        manual_attack = no
    }
}

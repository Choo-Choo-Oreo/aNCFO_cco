on_actions = {
	on_startup = {
		effect = {
			# Apply the Variables to States (Your existing scripts)
            # (Assuming SEH_species_effect iterates states to set variables)
            # If your scripts are Country-Scoped but set State variables, ensure they target states.
            # Below assumes your provided scripts set the variables correctly.
			every_country = {
				limit = { is_ai = no }
				SEH_species_effect  = yes
				SEH_ethnos_effect   = yes
				SEH_heritage_effect = yes
			}

			# Initialize Country Arrays and Apply Modifiers
			every_country = {
				SEH_initialize_country_acceptance = yes

				# OPTIONAL: Add specific starting acceptance here using IF/LIMIT
				# Example:
				# IF = {
				#	LIMIT = { tag = GER }
				#	SEH_add_accepted_species_full = { ID = 1 }
				# }

				# Calculate Modifiers for owned states
				SEH_refresh_all_owned_states = yes # Disabled until arrays set in countries history.
			}

			# every_country = {
			#	limit = { is_ai = yes }
			#	log = "[THIS.GetName]"
			#	every_owned_state = {
			#		log = "[THIS.GetID]"
			#	}
			# }
		}
	}

	# Dynamic Update on Control Change
	# ROOT = New Controller, FROM = Old Controller, FROM.FROM = State ID
	on_state_control_changed = {
		effect = {
			# Update the state's modifiers based on the New Controller (ROOT)
			SEH_update_state_acceptance_modifiers = yes
		}
	}
}

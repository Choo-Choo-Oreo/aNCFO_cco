on_actions = {
	on_startup = { # Initializing
		effect = { # Note to say what the effect does.
			log = "What Scope: [THIS.GetID] Example test to look at"
		}
	}

	on_daily = { # 7 per Week
	}

	on_weekly = { # 4 per Month
	}

	on_monthly = { # 12 per Year
	}
	# Read "\documentation\wikis\Wiki_OnAction.txt" for more details
}
